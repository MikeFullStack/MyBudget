
# See http://help.github.com/ignore-files/ for more about ignoring files.

# compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
/node_modules
npm-debug.log
yarn-error.log
testem.log

# IDEs and editors
.idea/
.project
.classpath
.cproject
.settings/
*.launch
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# System Files
.DS_Store
Thumbs.db

# Angular
.angular/
.cache/

# Firebase
.firebase/
firebase-debug.log
firebase-export-*

# Environment Configs (if they contain secrets)
# We keep the templates (prod/dev) but ignore the active copy if needed
# public/config.json 
# (In this project, config.json is generated, so we might want to ignore it)
public/config.json
